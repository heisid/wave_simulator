"mom i want double slit experiment"

"we have double slit experiment at home"

double slit experiment at home:

![screenshot](screenshot.png)

===============

o: toggle oscillator. ~if you turn off the oscillator and turn it on again, the oscillator will show up wherever the mouse cursor is.~

mouse scroll up/down: change oscillator period/frequency

r: reset state

click anywhere to create ripple

w: toggle walls. toggling this sometimes results wacky thing happens

===============

build and run:

- `make deps`
- `make run`
